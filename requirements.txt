# ============================================================================
# GBM-PharmaMind Python 依赖列表
# ============================================================================
# 安装方式：
#   pip install -r requirements.txt
#
# 注意：PyTorch 建议使用 CUDA 版本，请根据您的 CUDA 版本选择合适的安装命令：
#   CUDA 11.8: pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu121
#   CPU only:   pip install torch
# ============================================================================

# PyTorch（根据 CUDA 版本选择上述命令后，再安装其他依赖）
# torch==2.1.0

# 核心模型加载与微调
transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.30.0
sentencepiece>=0.1.99
protobuf>=4.25.0

# 化学信息学
rdkit>=2023.9.0

# 数据处理与可视化
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0
scikit-learn>=1.3.0

# 配置与日志
pyyaml>=6.0
tqdm>=4.65.0

# Jupyter（可选）
# jupyterlab>=4.0.0
